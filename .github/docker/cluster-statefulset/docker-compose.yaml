---
x-nifi-healthcheck: &nifi-healthcheck
  test:
    - CMD-SHELL
    - >-
      curl --fail --silent --show-error --max-time 10
      --cert "$${NIFI_HEALTHCHECK_CERT_DIR}/CN=admin_OU=NIFI.p12:$$(cat "$${NIFI_HEALTHCHECK_CERT_DIR}/CN=admin_OU=NIFI.password")"
      --cert-type P12 --cacert "$${NIFI_HEALTHCHECK_CERT_DIR}/nifi-cert.pem"
      https://$${HOSTNAME}:8080/nifi-api/controller/config > /dev/null
  interval: 5s
  timeout: 10s
  retries: 30
  start_period: 15s

services:
  nifi-toolkit:
    image: apache/nifi-toolkit:1.28.1@sha256:7a43836dcc7ac37c5243dd11792da3aaff1b28a1c0e06b41c23cb16118c42652
    container_name: nifi-toolkit
    entrypoint: /bin/bash
    command: /tmp/tls-scripts/nifi-gen-certs.sh
    volumes:
      - tls-cert:/tmp/tls-certs
      - tls-scripts:/tmp/tls-scripts
    environment:
      - KEYSTORE_PASSWORD_NIFI=${KEYSTORE_PASSWORD_NIFI}
      - KEYSTORE_PASSWORD_NIFI_REG=${KEYSTORE_PASSWORD_NIFI_REG}
      - TRUSTSTORE_PASSWORD=${TRUSTSTORE_PASSWORD}
      - IS_CLUSTER=true
  zookeeper:
    image: zookeeper:3.9.5@sha256:f258365d4882440a55dbedf8ee379152ed0be8006cc3641b81e6c1c1745c976b
    ports:
      - 127.0.0.1:2181:2181
      - 127.0.0.1:2182:2182
    volumes:
      - tls-cert:/opt/zookeeper/ssl
      - zk-configuration:/conf
    hostname: zookeeper
    environment:
      ZOO_MY_ID: 1
      ZOO_SERVERS: server.1=zookeeper:2888:3888
    healthcheck:
      test:
        - CMD-SHELL
        - nc -z 127.0.0.1 2182
      interval: 5s
      timeout: 5s
      retries: 12
  qubership-nifi-0:
    image: local-nifi:test
    depends_on:
      nifi-toolkit:
        condition: service_completed_successfully
      keycloak:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      consul:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
    volumes:
      - nifi-conf0:/opt/nifi/nifi-current/persistent_conf
      - tls-cert-nifi-0:/tmp/tls-certs-0
      - tls-cert-ca:/tmp/cert
      - tls-cert-zk:/tmp/cert-zk
    ports:
      - 127.0.0.1:8080:8080
    environment:
      - NAMESPACE=local
      - CONSUL_ENABLED=true
      - CONSUL_URL=consul:8500
      - NIFI_CONSUL_INT_FRAMEWORK=spring
      - AUTH=oidc
      - NIFI_NEW_SENSITIVE_KEY=${NIFI_SENSITIVE_KEY}
      - NIFI_WEB_HTTPS_PORT=8080
      - NIFI_HEALTHCHECK_CERT_DIR=/tmp/tls-certs-0
      - KEYSTORE_PATH=/tmp/tls-certs/keystore.p12
      - KEYSTORE_TYPE=PKCS12
      - KEYSTORE_PASSWORD=${KEYSTORE_PASSWORD_NIFI}
      - TRUSTSTORE_PATH=/tmp/tls-certs/truststore.p12
      - TRUSTSTORE_TYPE=PKCS12
      - TRUSTSTORE_PASSWORD=${TRUSTSTORE_PASSWORD}
      - INITIAL_ADMIN_IDENTITY=CN=admin, OU=NIFI
      - OIDC_CLIENT_ID=frontend
      - OIDC_CLIENT_SECRET=frontend
      - "OIDC_DISCOVERY_URL_NEW=https://keycloak:8443/realms/nifi-test-realm/.well-known/openid-configuration"
      - NIFI_CLUSTER_IS_NODE=true
      - ZOOKEEPER_ADDRESS=zookeeper:2182
      - NIFI_ZK_ROOT_NODE=/local/nifi
      - MICROSERVICE_NAME=qubership-nifi
      - NIFI_CLUSTER_NODE_PROTOCOL_PORT=8082
      - NIFI_ELECTION_MAX_WAIT=30 secs
      - NIFI_WEB_PROXY_HOST=localhost:8080,localhost:9080,localhost:10080
      - IS_STATEFUL_SET=true
      - ZOOKEEPER_SSL_ENABLED=true
    container_name: local-nifi
    healthcheck: *nifi-healthcheck
    hostname: qubership-nifi-0
    restart: unless-stopped
  qubership-nifi-1:
    image: local-nifi:test
    depends_on:
      nifi-toolkit:
        condition: service_completed_successfully
      keycloak:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      consul:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
    volumes:
      - nifi-conf1:/opt/nifi/nifi-current/persistent_conf
      - tls-cert-nifi-1:/tmp/tls-certs-1
      - tls-cert-ca:/tmp/cert
      - tls-cert-zk:/tmp/cert-zk
    ports:
      - 127.0.0.1:9080:8080
    environment:
      - NAMESPACE=local
      - CONSUL_ENABLED=true
      - CONSUL_URL=consul:8500
      - NIFI_CONSUL_INT_FRAMEWORK=spring
      - AUTH=oidc
      - NIFI_NEW_SENSITIVE_KEY=${NIFI_SENSITIVE_KEY}
      - NIFI_WEB_HTTPS_PORT=8080
      - NIFI_HEALTHCHECK_CERT_DIR=/tmp/tls-certs-1
      - KEYSTORE_PATH=/tmp/tls-certs/keystore.p12
      - KEYSTORE_TYPE=PKCS12
      - KEYSTORE_PASSWORD=${KEYSTORE_PASSWORD_NIFI}
      - TRUSTSTORE_PATH=/tmp/tls-certs/truststore.p12
      - TRUSTSTORE_TYPE=PKCS12
      - TRUSTSTORE_PASSWORD=${TRUSTSTORE_PASSWORD}
      - INITIAL_ADMIN_IDENTITY=CN=admin, OU=NIFI
      - OIDC_CLIENT_ID=frontend
      - OIDC_CLIENT_SECRET=frontend
      - "OIDC_DISCOVERY_URL_NEW=https://keycloak:8443/realms/nifi-test-realm/.well-known/openid-configuration"
      - NIFI_CLUSTER_IS_NODE=true
      - ZOOKEEPER_ADDRESS=zookeeper:2182
      - NIFI_ZK_ROOT_NODE=/local/nifi
      - MICROSERVICE_NAME=qubership-nifi
      - NIFI_CLUSTER_NODE_PROTOCOL_PORT=8082
      - NIFI_ELECTION_MAX_WAIT=30 secs
      - NIFI_WEB_PROXY_HOST=localhost:8080,localhost:9080,localhost:10080
      - IS_STATEFUL_SET=true
      - ZOOKEEPER_SSL_ENABLED=true
    container_name: qubership-nifi-1
    healthcheck: *nifi-healthcheck
    hostname: qubership-nifi-1
    restart: unless-stopped
  qubership-nifi-2:
    image: local-nifi:test
    depends_on:
      nifi-toolkit:
        condition: service_completed_successfully
      keycloak:
        condition: service_healthy
      postgresql:
        condition: service_healthy
      consul:
        condition: service_healthy
      zookeeper:
        condition: service_healthy
    volumes:
      - nifi-conf2:/opt/nifi/nifi-current/persistent_conf
      - tls-cert-nifi-2:/tmp/tls-certs-2
      - tls-cert-ca:/tmp/cert
      - tls-cert-zk:/tmp/cert-zk
    ports:
      - 127.0.0.1:10080:8080
    environment:
      - NAMESPACE=local
      - CONSUL_ENABLED=true
      - CONSUL_URL=consul:8500
      - NIFI_CONSUL_INT_FRAMEWORK=spring
      - AUTH=oidc
      - NIFI_NEW_SENSITIVE_KEY=${NIFI_SENSITIVE_KEY}
      - NIFI_WEB_HTTPS_PORT=8080
      - NIFI_HEALTHCHECK_CERT_DIR=/tmp/tls-certs-2
      - KEYSTORE_PATH=/tmp/tls-certs/keystore.p12
      - KEYSTORE_TYPE=PKCS12
      - KEYSTORE_PASSWORD=${KEYSTORE_PASSWORD_NIFI}
      - TRUSTSTORE_PATH=/tmp/tls-certs/truststore.p12
      - TRUSTSTORE_TYPE=PKCS12
      - TRUSTSTORE_PASSWORD=${TRUSTSTORE_PASSWORD}
      - INITIAL_ADMIN_IDENTITY=CN=admin, OU=NIFI
      - OIDC_CLIENT_ID=frontend
      - OIDC_CLIENT_SECRET=frontend
      - "OIDC_DISCOVERY_URL_NEW=https://keycloak:8443/realms/nifi-test-realm/.well-known/openid-configuration"
      - NIFI_CLUSTER_IS_NODE=true
      - ZOOKEEPER_ADDRESS=zookeeper:2182
      - NIFI_ZK_ROOT_NODE=/local/nifi
      - MICROSERVICE_NAME=qubership-nifi
      - NIFI_CLUSTER_NODE_PROTOCOL_PORT=8082
      - NIFI_ELECTION_MAX_WAIT=30 secs
      - NIFI_WEB_PROXY_HOST=localhost:8080,localhost:9080,localhost:10080
      - IS_STATEFUL_SET=true
      - ZOOKEEPER_SSL_ENABLED=true
    container_name: qubership-nifi-2
    healthcheck: *nifi-healthcheck
    hostname: qubership-nifi-2
    restart: unless-stopped
  nifi-registry:
    image: ghcr.io/netcracker/qubership-nifi-registry:latest
    depends_on:
      nifi-toolkit:
        condition: service_completed_successfully
      keycloak:
        condition: service_healthy
      postgresql:
        condition: service_healthy
    volumes:
      - tls-cert-reg:/tmp/tls-certs
      - tls-cert-ca:/tmp/cert
    ports:
      - 127.0.0.1:18080:18080
    environment:
      - AUTH=oidc
      - NIFI_REGISTRY_WEB_HTTPS_PORT=18080
      - NIFI_REGISTRY_WEB_HTTP_HOST=0.0.0.0
      - KEYSTORE_PATH=/tmp/tls-certs/keystore.p12
      - KEYSTORE_TYPE=PKCS12
      - KEYSTORE_PASSWORD=${KEYSTORE_PASSWORD_NIFI_REG}
      - TRUSTSTORE_PATH=/tmp/tls-certs/truststore.p12
      - TRUSTSTORE_TYPE=PKCS12
      - TRUSTSTORE_PASSWORD=${TRUSTSTORE_PASSWORD}
      - INITIAL_ADMIN_IDENTITY=CN=admin, OU=NIFI
      - OIDC_CLIENT_ID=frontend
      - OIDC_CLIENT_SECRET=frontend
      - "OIDC_DISCOVERY_URL_NEW=https://keycloak:8443/realms/nifi-test-realm/.well-known/openid-configuration"
      - NIFI_REGISTRY_CHECK_HOST=nifi-registry
    container_name: local-nifi-registry-cluster
    hostname: nifi-registry
    healthcheck:
      interval: 5s
      retries: 30
      start_period: 15s
      timeout: 10s
      test:
        - "CMD"
        - "bash"
        - "/opt/nifi-registry/scripts/ready.sh"
    restart: unless-stopped
  consul:
    image: hashicorp/consul:1.20@sha256:200471c74bc0965cd20c81eacc31a209873d5d9e599fe637ceb856b0cbbc518d
    ports:
      - 127.0.0.1:8500:8500
    hostname: consul
    healthcheck:
      test:
        - "CMD-SHELL"
        - "wget -q -O - http://127.0.0.1:8500/v1/status/leader | grep -q ':'"
      start_period: 5s
      interval: 5s
      timeout: 3s
      retries: 12
  keycloak:
    image: keycloak/keycloak:26.1.4@sha256:044a457e04987e1fff756be3d2fa325a4ef420fa356b7034ecc9f1b693c32761
    hostname: keycloak
    restart: on-failure:3
    depends_on:
      postgresql:
        condition: service_healthy
    volumes:
      - keycloak-init:/opt/keycloak/data/import
      - tls-cert:/tmp/tls-certs
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgresql:5432/postgres
      KC_DB_USERNAME: postgres
      KC_DB_PASSWORD: ${DB_PASSWORD}
      KC_HTTPS_KEY_STORE_FILE: /tmp/tls-certs/keycloak-server.p12
      KC_HTTPS_KEY_STORE_PASSWORD: ${KEYCLOAK_TLS_PASS}
      KC_HTTPS_PORT: 8443
      KC_HTTP_ENABLED: false
    ports:
      - 28080:8080
      - 28443:8443
    command:
      - start-dev
      - "--import-realm"
      - "--hostname"
      - "https://localhost:28443"
      - "--hostname-backchannel-dynamic"
      - "true"
    healthcheck:
      test:
        - CMD
        - bash
        - -c
        - exec 3<>/dev/tcp/127.0.0.1/8443
      interval: 5s
      timeout: 5s
      retries: 24
      start_period: 15s
  postgresql:
    image: postgres:16.8@sha256:301bcb60b8a3ee4ab7e147932723e3abd1cef53516ce5210b39fd9fe5e3602ae
    ports:
      - "127.0.0.1:5430:5432"
    volumes:
      - pg-db:/var/lib/postgresql/data/pgdata
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - PGDATA=/var/lib/postgresql/data/pgdata
    container_name: postgresql
    hostname: postgresql
    healthcheck:
      test:
        - CMD
        - pg_isready
        - -U
        - postgres
      interval: 5s
      timeout: 5s
      retries: 12
volumes:
  tls-scripts:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: ${GIT_BASE_DIR:-$BASE_DIR}/.github/workflows/sh/
  tls-cert:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/tls-cert/
  tls-cert-nifi-0:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/tls-cert/qubership-nifi-0/
  tls-cert-nifi-1:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/tls-cert/qubership-nifi-1/
  tls-cert-nifi-2:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/tls-cert/qubership-nifi-2/
  tls-cert-zk:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/tls-cert/
  tls-cert-reg:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/tls-cert/nifi-registry/
  tls-cert-ca:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/tls-cert/ca/
  nifi-conf0:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/nifi-0/per-conf/
  nifi-conf1:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/nifi-1/per-conf/
  nifi-conf2:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/nifi-2/per-conf/
  pg-db:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/pg-db
  keycloak-init:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: ${GIT_BASE_DIR:-$BASE_DIR}/.github/configuration/keycloak/
  zk-configuration:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: $BASE_DIR/temp-vol/zk-conf/
